@model features.Models.Symptom

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="container" style="margin-top: 150px;">
    <h3>Are you sure you want to delete this Symptom?</h3>
    <div>
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.symptName)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.symptName)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Department.dptName)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Department.dptName)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color button">
                <input type="submit" value="Delete" class="btn button-delete" />
                @Html.ActionLink("Back to Symptoms", "Index")
            </div>
        }
    </div>
</div>